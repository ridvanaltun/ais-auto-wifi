# AIS Wi-Fi Auto-Login — dependencies
# Install:  pip3 install -r requirements.txt

# Menu bar (tray) interface
rumps>=0.4.0

# HTTP requests (captive portal probing + form submission)
requests>=2.28

# Parsing the HTML form on the login page
beautifulsoup4>=4.11

# Storing credentials in the macOS Keychain
keyring>=24.0

# For reading the SSID (with Location permission). rumps already pulls in
# pyobjc; the CoreWLAN framework is needed separately for the SSID.
pyobjc-framework-CoreWLAN>=9.0

# "Open at Login" (macOS login item via SMAppService).
pyobjc-framework-ServiceManagement>=9.0
